services:
  postgres:
    image: postgres:16
    container_name: postgres-pop
    environment:
      POSTGRES_DB: pop_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: